@model IEnumerable<BrothersGYM.Models.DetallePedido>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PrecioUnidad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cantidad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descuento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdpedidoNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdproductoNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PrecioUnidad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cantidad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descuento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdpedidoNavigation.Direccion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdproductoNavigation.Imagen)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IddetalleProducto">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IddetalleProducto">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IddetalleProducto">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
