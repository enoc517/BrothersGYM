@model IEnumerable<BrothersGYM.Models.Puesto>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PagHora)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IddepartamentoNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PagHora)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IddepartamentoNavigation.Nombre)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Idpuesto">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Idpuesto">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Idpuesto">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
