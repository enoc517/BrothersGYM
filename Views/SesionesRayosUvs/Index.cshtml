@model IEnumerable<BrothersGYM.Models.SesionesRayosUv>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdclienteNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PaqueteSesiones)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdclienteNavigation.Apellido)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PaqueteSesiones.PaqueteSesioensId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Idsesiones">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Idsesiones">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Idsesiones">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
